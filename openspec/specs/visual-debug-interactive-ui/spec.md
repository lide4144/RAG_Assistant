# visual-debug-interactive-ui 规范

## 目的
待定 - 由归档变更 m8-5-visual-debug-interactive-ui 创建。归档后请更新目的。
## 需求
### 需求:系统必须提供可运行的双区调试 UI
系统必须保留开发者审查面板能力，但默认在用户主流程中折叠或隐藏，避免干扰普通科研用户。

#### 场景:默认视图聚焦用户任务
- **当** 普通用户进入 Chat 页面
- **那么** 主界面必须优先展示对话与证据/卡片区，调试面板不作为默认主视觉

### 需求:系统必须支持回答引用的可交互审查
系统必须继续支持引用点击审查能力，并允许在用户友好视图中查看“证据来源”而不暴露底层冗余字段。

#### 场景:用户视图查看证据来源
- **当** 用户点击回答中的引用标记
- **那么** 系统必须展示可读证据来源（论文、位置、片段），且无需进入开发者模式

### 需求:系统必须支持新对话与上下文清空
UI 必须提供“开启新对话”或“清空上下文”操作，并且必须调用 `clear_session(session_id)`。调用成功后，新问题处理必须基于空历史上下文，禁止复用被清空会话的历史。

#### 场景:清空后不污染新检索
- **当** 用户点击“开启新对话”后立即发起无关问题
- **那么** 系统必须按新会话处理，且回答与检索上下文不得依赖清空前历史

### 需求:系统必须高亮 graph_expand 与降级告警
审查面板中的 `evidence_grouped` 必须高亮展示 `score_retrieval`、`score_rerank`、`source`；当 `source=graph_expand` 时必须有明确视觉标识。若 `decision` 为 `refuse` 或 `clarify`，系统必须使用醒目颜色展示 `reason` 与 `output_warnings`。当本轮命中控制意图路由时，审查面板还必须展示 `topic_match` 的查询来源，明确区分“用户原句”与“锚定主题”。

#### 场景:图扩展证据可区分
- **当** 某条 evidence 的 `source=graph_expand`
- **那么** 审查面板必须显示可识别的 graph_expand 标签，并可与 BM25/Dense 来源区分

#### 场景:降级告警高亮
- **当** Sufficiency Gate 输出 `decision=refuse` 或 `decision=clarify`
- **那么** 审查面板必须以醒目颜色展示 `reason` 与 `output_warnings`

#### 场景:主题匹配来源可审查
- **当** 本轮 `intent_type` 为控制意图且 Gate 执行了 topic match
- **那么** 审查面板必须显示 `topic_query_source`，用于确认 Gate 是否基于锚定主题计算

### 需求:系统必须展示控制意图路由与主题锚定审查信息
调试 UI 必须展示本轮 `intent_type`、`anchor_query`（若有）、`topic_query_source`（例如 `user_query` 或 `anchor_query`）以及路由开关状态，便于定位控制意图误路由问题。

#### 场景:控制意图字段可见
- **当** 用户输入“用中文回答我”并触发控制意图路由
- **那么** 审查面板必须可见 `intent_type=style_control` 与对应 `anchor_query`

### 需求:系统必须将开发者审查面板与产品界面解耦
系统必须允许在产品界面与开发者审查面板间独立开关；禁止默认向普通用户暴露调试字段。

#### 场景:普通用户模式隐藏审查字段
- **当** UI 运行在普通用户模式
- **那么** 页面必须隐藏 trace 原始字段，仅展示用户可理解信息

### 需求:系统必须支持 GraphRAG 调试可视化入口
开发者审查面板必须支持查看图扩展子图、路径与节点元数据，作为 `graph_expand` 标签的增强视图。

#### 场景:开发者审查中查看图路径
- **当** 当前轮存在图扩展证据
- **那么** 审查面板必须显示至少一条从 seed 到扩展节点的路径

