## 1. 共享端点解析

- [x] 1.1 在前端新增共享端点解析工具，统一处理 `NEXT_PUBLIC_KERNEL_BASE_URL`、`NEXT_PUBLIC_GATEWAY_WS_URL` 的优先级和默认回退逻辑。
- [x] 1.2 为共享端点解析工具实现基于 `window.location` 的 WebSocket 协议推导，确保 `http -> ws`、`https -> wss`。

## 2. 页面接入

- [x] 2.1 更新聊天页连接逻辑，使运行态接口默认走同域受控路径，聊天 WebSocket 默认走当前站点来源推导地址。
- [x] 2.2 更新知识处理页连接逻辑，使任务事件 WebSocket 默认走当前站点来源推导地址。
- [x] 2.3 更新全局壳层运行态加载逻辑，未显式配置 Kernel 地址时默认使用同域相对 `/api/admin/*` 路径。

## 3. 验证与文档

- [x] 3.1 补充前端测试，覆盖未配置地址时的同域默认值、HTTPS 对应 WSS、以及显式环境变量覆盖默认值。
- [x] 3.2 更新启动与部署文档，明确区分本地联调注入地址与远程部署/反向代理的推荐配置方式。
