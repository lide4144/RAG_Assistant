## 1. 结构化标题与语义建模

- [x] 1.1 扩展 `app/marker_parser.py` 的中间表示，输出带层级语义的标题候选集合，至少区分 `H1`、`H2` 与 markdown 首行来源
- [x] 1.2 扩展成功解析后的统一中间表示，确保 `block_type` 与 markdown 来源可用性不会在归一化阶段静默丢失，并可供 chunking/retrieval/citation 消费
- [x] 1.3 盘点并收紧当前标题候选抽取规则，确保结构化候选不会被简单平铺后丢失来源语义
- [x] 1.4 为结构化候选新增样例测试，覆盖“论文主标题 + 摘要头 + 章节标题”同时存在的 PDF 片段，以及表格/公式 block 类型保留场景

## 2. 标题决策树重构

- [x] 2.1 重构 `app/parser.py` 的标题选择逻辑为“分层尝试、层内门禁、层间降级”的结构化信任决策树
- [x] 2.2 保持现有黑名单、作者尾巴裁剪和置信度阈值兼容，并验证它们只在当前优先层内生效
- [x] 2.3 为标题决策新增回归测试，覆盖“Marker 成功但旧逻辑会误选章节标题”和“高优先层被拒绝后正确降级”两类场景
- [x] 2.4 为成功解析但结构语义仅部分消费的路径补诊断断言，确保“存在但未消费”和“不存在”可以区分

## 3. Chunking 与入库链路接入

- [x] 3.1 在 `app/ingest.py` 接入新的结构化标题决策结果，保证 `papers.json` 与 ingest 报告落盘新的来源语义
- [x] 3.2 调整 `app/chunker.py` 与相关 chunk 数据结构，使表格/公式等结构块在 chunk 层保留可区分类型
- [x] 3.3 在 ingest 元数据或报告中落盘 block 语义保留状态与 markdown 消费状态，便于后续排查和链路审计
- [x] 3.4 调整历史标题修复脚本，使其优先复用结构化候选或支持安全重跑 Marker，而不是仅复用当前落盘标题
- [x] 3.5 为无法获取结构化候选的历史论文定义显式失败/跳过路径，并补对应测试或脚本输出断言

## 4. Retrieval 与 Citation 贯通

- [x] 4.1 在 `app/retrieve.py` 或等价链路中接入结构块语义的 retrieval weighting，定义表格/公式 chunk 的加权或门控策略
- [x] 4.2 在 `app/qa.py` 与 citation 组装链路中保留结构化 provenance，使表格/公式证据不再被完全扁平为普通正文 citation
- [x] 4.3 为结构化 citation contract 补回归测试，验证旧消费者基础字段兼容、新消费者可读取结构 provenance

## 5. 可观测性与验收

- [x] 5.1 扩展 ingest 诊断字段，记录被采用的标题层级、结构化候选被拒绝原因、block 语义保留状态与最终降级路径
- [x] 5.2 准备一组标题误选回归样本，验证结构化信任方案能修复已知样本且不会大面积退化为 `Untitled Paper`
- [x] 5.3 准备至少一组“Marker 成功且表格/公式语义贯通到 retrieval weighting 与 citation”的验收样本
- [x] 5.4 更新相关运维或调试文档，说明新的标题来源语义、结构语义保留状态、retrieval/citation 行为变化、修复前提和排查方法
