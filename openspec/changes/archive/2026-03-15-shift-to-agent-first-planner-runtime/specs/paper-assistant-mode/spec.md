## ADDED Requirements

### 需求:系统必须将论文助理模式纳入 planner runtime 的统一调度
系统必须将论文助理模式纳入顶层 planner runtime 的统一调度边界；planner 必须决定何时进入研究辅助能力、何时先澄清、何时回退到普通事实问答或总结路径；禁止将论文助理模式实现为绕过 planner runtime 的独立旁路入口。

#### 场景:研究辅助请求先经过 planner 决策
- **当** 用户提出“帮我比较这些论文并给出下一步研究建议”之类的研究辅助请求
- **那么** 系统必须先由 planner runtime 判断是否进入论文助理能力，而不是直接跳入某个旁路模式

### 需求:系统必须保证论文助理输出仍受 kernel 安全边界约束
即使 planner 选择了论文助理能力，系统仍必须通过 kernel 的 evidence gate、citation contract 与任务状态约束生成最终输出；禁止将研究辅助模式视为可以放宽证据约束或跳过可追溯性的特例。

#### 场景:论文助理回答仍需可追溯证据
- **当** planner runtime 选择论文助理能力生成主题总结、差异比较或后续建议
- **那么** 最终回答必须仍然带有可追溯证据与引用，而不是仅输出无法审计的自由生成结论

## MODIFIED Requirements

### 需求:系统必须提供论文助理默认模式
系统必须将默认回答组织为“主题方向 + 可追溯证据 + 下一步可问问题”的用户友好结构，并将该结构视为 planner runtime 可调度的高层研究辅助能力输出；禁止仅输出面向开发调试的结构化字段，也禁止把论文助理模式实现为脱离 planner/runtime 安全边界的独立产品模式。

#### 场景:planner 选择论文助理能力后输出三段式结构
- **当** planner runtime 判定当前请求适合进入论文助理能力
- **那么** 回答必须至少包含主题方向总结、对应证据引用和后续追问建议

### 需求:系统必须在证据不足时执行最小澄清
系统必须保持最小澄清策略（一次仅一条澄清问题），且该澄清必须由 planner runtime 统一决定并面向科研任务语义；当研究辅助能力前置条件不满足时，禁止绕过 planner 直接进入自由生成回答。

#### 场景:研究辅助前置条件不足时先澄清
- **当** planner runtime 判定用户意图属于研究辅助，但缺少主题、论文范围或实验约束
- **那么** 系统必须输出 1 条面向科研任务语义的澄清问题，而不是直接扩写低依据内容
