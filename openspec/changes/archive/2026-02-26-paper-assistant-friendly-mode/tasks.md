## 1. 路由与重写策略

- [x] 1.1 在 `app/rewrite.py` 与 `app/qa.py` 中增加“开放式总结意图”识别分支，保证自然提问优先走检索问答路径。
- [x] 1.2 在 rewrite 输入处理中实现“约束去污染”逻辑：当前轮为开放式总结时降权上一轮 `clarify` 约束（如 numeric/subject_constraint）。
- [x] 1.3 在 trace 中新增并输出去污染相关字段（如 `history_constraint_dropped`、`dropped_constraints`）。

## 2. Sufficiency Gate 与助理模式

- [x] 2.1 在 `app/sufficiency.py` 中实现最小澄清优先策略：可补齐缺口时仅输出 1 条澄清问题。
- [x] 2.2 增加 `assistant_summary`（或等价策略）分支：对开放式总结问题在可追溯条件下优先输出主题化总结与建议追问。
- [x] 2.3 调整关键要素缺口判定，避免对未请求指标的开放式总结误触发 numeric 拒答。
- [x] 2.4 修复“assistant_mode_used=true 但仍直接 refuse”路径，确保助理模式命中时优先产出部分可追溯回答或最小澄清。
- [x] 2.5 修复 `topic_mismatch` 误触发：当检索命中同主题论文簇时，Gate 不得直接拒答，需进入部分回答或单问题澄清。
- [x] 2.6 增加连续澄清上限（默认 2）与强制降级回答策略：达到上限后禁止继续 `clarify`，必须给出低置信可追溯回答。
- [x] 2.7 增加“主题切换后重置澄清计数”逻辑，避免跨主题错误继承澄清上限状态。

## 3. 会话状态与上下文隔离

- [x] 3.1 在 `app/session_state.py` 增加“主题锚点”和“临时澄清约束”分离存储结构。
- [x] 3.2 在多轮请求组装中实现“保留主题锚点、降权临时约束”的应用规则，并保留回退机制。
- [x] 3.3 为会话重置后首轮请求增加显式审查字段，确认不会继承旧会话约束。

## 4. 可观测性与验收

- [x] 4.1 在 `run_trace/qa_report` 中补充助理模式命中、澄清次数、门控触发原因等指标字段。
- [x] 4.2 在 UI（`app/ui.py`）侧边栏增加新字段展示，便于人工审查“为何澄清/拒答/助理总结”。
- [x] 4.3 增加端到端回归用例：自然首问总结、多轮追问建议、会话清空后无污染、语料外问题拒答。
- [x] 4.4 新增“自然口语两轮回归”固定用例，强制校验第 1 轮与第 2 轮均不出现模板化拒答文案。
- [x] 4.5 新增“三轮连续口语回归”固定用例：验证第 3 轮不会继续澄清，必须产出低置信可追溯回答。
- [x] 4.6 在 trace 中新增/校验 `clarify_count`、`clarify_limit_hit`、`forced_partial_answer` 字段，并确保与最终决策一致。

## 5. 回归与发布准备

- [x] 5.1 对比新旧策略在“自然提问场景”的拒答率、澄清率与可追溯回答率，形成回归报告。
- [x] 5.2 校验新增字段与现有日志 schema 兼容，避免破坏现有报告解析。
- [x] 5.3 预留配置开关与回滚路径，确保可快速回退到旧门控策略。
- [x] 5.4 设定发布门槛：自然首问拒答率低于阈值，且多轮追问至少返回一次可追溯主题化回答。
- [x] 5.5 更新发布门槛：同主题会话不得连续超过 2 轮 `clarify`，并在第 3 轮强制产出可追溯回答或明确风险拒答。
