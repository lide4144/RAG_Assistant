## 1. Nginx 生产反代落地

- [x] 1.1 新增项目内可复用的 Nginx 配置模板，覆盖 `/`、`/api/admin/*`、`/api/library/*`、`/api/tasks/*` 与 `/ws` 代理
- [x] 1.2 为 WebSocket 代理补齐升级头、超时和主机头转发配置
- [x] 1.3 新增 Cloud Studio 单端口入口模板或启动脚本，避免平台暴露多个应用端口

## 2. 前端与服务入口对齐

- [x] 2.1 确认前端生产访问默认走同域 `/api/*` 和 `/ws`，避免浏览器依赖 `8000/8080` 绝对地址
- [x] 2.2 如有缺口，补齐 Next rewrite 或相关端点解析逻辑，使聊天、设置和知识处理页都能通过同域反代访问后端

## 3. 部署文档与验证

- [x] 3.1 更新 README 或部署文档，新增生产环境 Nginx 反代说明与环境变量约定
- [x] 3.2 补充最小验证步骤和排障指引，覆盖 HTTP API、WebSocket 与页面访问
- [x] 3.3 补充 Cloud Studio 应用封装场景的单端口运行说明
