## 1. 运行目录唯一性加固

- [x] 1.1 更新 `app/runlog.py` 的 `create_run_dir`，将目录粒度提升到秒级并在冲突时追加递增后缀
- [x] 1.2 增加同秒连续创建目录的验证用例，确保不会复用同一路径
- [x] 1.3 在运行报告中记录最终使用的运行目录，便于回溯与排障

## 2. 配置解析升级

- [x] 2.1 引入标准 YAML 解析依赖并在 `app/config.py` 中改用 `safe_load` 读取配置
- [x] 2.2 保留默认配置回退机制，确保缺失字段时仍可稳定运行
- [x] 2.3 处理 YAML 解析失败路径，输出清晰错误或安全回退行为

## 3. chunk 参数运行时校验

- [x] 3.1 新增 `chunk_size` 与 `overlap` 校验逻辑，约束 `300 <= chunk_size <= 500` 且 `0 <= overlap < chunk_size`
- [x] 3.2 对非法配置值记录告警并回退到安全默认值（400/50）
- [x] 3.3 在 `app/ingest.py` 接入校验后的配置并确保切块逻辑使用生效值

## 4. 验证与回归

- [x] 4.1 运行 ingest 冒烟验证，确认输出文件结构与 CLI 行为保持兼容
- [x] 4.2 验证 run trace 与 ingest report 在新目录策略下仍正确落盘
- [x] 4.3 补充或更新文档，说明新增依赖与配置校验/回退行为
