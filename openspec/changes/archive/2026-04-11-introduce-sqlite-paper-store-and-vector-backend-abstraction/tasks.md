## 1. SQLite 论文域基础

- [x] 1.1 设计并创建 SQLite 论文域 schema，覆盖论文主记录、阶段状态、topic 归属、chunk 归属与索引/产物状态
- [x] 1.2 实现论文域存储访问层，提供按 `paper_id` 查询、按状态筛选、写入阶段状态与更新产物状态的基础接口
- [x] 1.3 为论文主记录定义稳定来源字段与约束，避免继续把临时导入目录路径写成长期主记录路径

## 2. 历史数据迁移与兼容导出

- [x] 2.1 实现从 `papers.json`、`library_topics.json`、`chunks*.jsonl`、`paper_summary.json`、`structure_index.json` 导入 SQLite 的迁移逻辑
- [x] 2.2 为历史记录添加来源修复与异常标记逻辑，识别无法可靠恢复的路径或归属关系
- [x] 2.3 实现从 SQLite 生成兼容导出文件的路径，明确 `papers.json` 与相关文件降级为派生产物

## 3. 导入与生命周期接入

- [x] 3.1 改造导入流程，使去重、导入、解析、清洗、索引与图构建阶段都回写单篇论文生命周期状态
- [x] 3.2 改造导入流程，使新论文优先写入 SQLite 再生成清洗、summary、structure 与兼容导出产物
- [x] 3.3 为删除、失败重试与重建编排接入论文级状态，支持按 `paper_id` 标记 `failed`、`deleted` 与 `rebuild_pending`

## 4. 向量后端抽象与首个文件后端

- [x] 4.1 定义统一向量后端接口，覆盖 upsert、按 `paper_id` 删除、检索、重建与后端元信息查询
- [x] 4.2 将现有文件向量索引改造成第一个后端实现，并保持当前本地检索链路可继续运行
- [x] 4.3 将当前后端类型、构建时间、语料快照与过期状态回写 SQLite，为未来切换到 Qdrant 预留稳定元数据边界

## 5. API、目录与运行态切换

- [x] 5.1 改造目录与状态查询 API，使论文列表、论文状态与导入后可见信息优先读取 SQLite 权威记录
- [x] 5.2 改造后端聚合逻辑，使批任务视图能关联到单篇论文级状态而不是仅返回临时 `recent_items`
- [x] 5.3 为后续 planner 与前端升级提供稳定响应契约，确保 `paper_id`、状态与后端元信息在 API 层可复用

## 6. 验证、回滚与升级基础

- [x] 6.1 为 SQLite 迁移、兼容导出、论文级状态流转与向量后端抽象补充单元测试与集成测试
- [x] 6.2 验证现有文件检索模式在新抽象下仍可完成导入、目录筛选、检索与问答主路径
- [x] 6.3 编写迁移与回滚说明，明确如何在保留文件产物的前提下重建数据库，并为后续 `add-qdrant-vector-backend` 变更保留可接入的升级链路
