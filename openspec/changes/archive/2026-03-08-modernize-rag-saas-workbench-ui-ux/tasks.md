## 1. 状态映射与全局约束

- [x] 1.1 新增 `StatusMapper` 工具，定义状态到文案/图标/语义色映射（含 unknown fallback）
- [x] 1.2 在前端渲染层替换直接状态枚举展示，统一改为 `StatusMapper` 输出
- [x] 1.3 全局排查并移除页面 `JSON.stringify` 直出路径，补充开发模式受控调试开关

## 2. 全局壳层与知识处理页改造

- [x] 2.1 将顶部降级提示重构为可折叠 Alert，默认折叠并支持“查看详情”列表展示 fallback 参数
- [x] 2.2 将连接状态重构为无边框状态指示器（绿色发光圆点 + `🟢 已连接` 文案）
- [x] 2.3 重构知识处理页统计卡，应用 `tabular-nums` 与高对比数字层级
- [x] 2.4 将四阶段卡片重构为横向步骤条并接入 `StatusMapper` 状态映射
- [x] 2.5 美化 Graph Build 日志区为终端风格（深色背景、等宽字体、可读滚动条）
- [x] 2.6 优化导入历史 `Run` 列：短 ID、Tooltip 全值、一键复制交互

## 3. 模型设置页与聊天页改造

- [x] 3.1 实现辅助模型“继承全局配置”默认行为并隐藏独立凭据输入
- [x] 3.2 增加 `独立配置 (Override)` 开关并在开启时展开 `api_base/api_key` 输入
- [x] 3.3 删除模型设置页底部“已保存值/生效值”原始 JSON 区块
- [x] 3.4 增加字段级“⚠️ 未保存”徽章逻辑，保存成功后自动清除
- [x] 3.5 为聊天页空状态增加 3-4 个快捷指令卡并实现点击即发
- [x] 3.6 将聊天顶部模型摘要重构为语义标签组（如推理模型/重写模型）

## 4. 验收与回归

- [x] 4.1 补充单元测试：StatusMapper 映射、Override 条件渲染、未保存状态计算
- [x] 4.2 补充 E2E 流程：知识处理步骤条与历史表交互、聊天空态快捷发送
- [x] 4.3 执行视觉回归检查并修复关键断点下的排版问题（桌面端/移动端）
- [x] 4.4 更新变更验收清单，逐项对照 Epic AC 打勾确认
