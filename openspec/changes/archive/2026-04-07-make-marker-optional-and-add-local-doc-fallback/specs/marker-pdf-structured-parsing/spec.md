## 新增需求
<!-- 无 -->

## 修改需求

### 需求:本地 Marker 解析能力
系统必须将 Marker 定义为本地 PDF 的可选增强解析器，而不是默认必经主路径。系统必须仅在 `marker_enabled=true` 且运行环境满足前置条件时尝试 Marker 结构化解析；当 Marker 未启用、依赖缺失、执行超时或解析失败时，系统必须回到基础 PDF 解析器并保证导入可完成。

#### 场景:Marker 未启用时直接走基础解析
- **当** 本地配置中 `marker_enabled=false`
- **那么** 系统必须直接使用基础 PDF 解析器完成导入，且不得将该路径标记为异常降级

#### 场景:Marker 启用但失败时受控降级
- **当** `marker_enabled=true` 且 Marker 依赖缺失、执行超时或解析报错
- **那么** 系统必须记录增强解析失败原因并自动回退到基础解析器，不得中断整批导入

### 需求:解析可观测性字段
系统必须在 ingest 运行报告中区分“基础解析直接完成”和“增强解析失败后降级完成”，至少包含当前解析模式、是否发生增强降级与可定位失败原因；系统禁止把 `marker_enabled=false` 的基础解析路径误记为 `fallback error`。

#### 场景:基础解析为正常主路径
- **当** 文档在 `marker_enabled=false` 下完成导入
- **那么** ingest 报告必须将其记录为基础解析完成，而不是 `parser_fallback=true`

#### 场景:增强降级路径可审计
- **当** 文档在尝试 Marker 后回退到基础解析器
- **那么** ingest 报告必须标记其为增强降级完成，并记录具体失败原因与阶段

## 移除需求
<!-- 无 -->
