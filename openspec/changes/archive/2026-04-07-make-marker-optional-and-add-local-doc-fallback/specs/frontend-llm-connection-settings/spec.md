## 新增需求
<!-- 无 -->

## 修改需求

### 需求:系统必须支持保存最终配置
系统必须在模型设置页中提供 Marker 总开关，并允许管理员显式决定是否启用 Marker 结构化解析；当 Marker 关闭时，系统必须仍允许保留既有 tuning 与 LLM 增强配置，但这些配置不得被视为当前生效。保存后页面必须回显当前开关状态与最终生效模式。

#### 场景:关闭 Marker 后保存基础解析模式
- **当** 管理员关闭 Marker 总开关并保存配置
- **那么** 系统必须回显当前模式为 `基础解析`，且不得继续将 Marker 视为默认导入主路径

#### 场景:开启 Marker 后进入增强解析模式
- **当** 管理员开启 Marker 总开关并保存配置
- **那么** 系统必须回显当前模式为 `增强解析`，并允许后续导入尝试 Marker 结构化解析

### 需求:系统必须提供模型连接设置的保存反馈
系统必须在“测试并保存”操作期间显示加载状态，并必须在保存成功后给出 Toast 成功提示；对于 Marker 总开关，系统还必须在保存后显示当前配置是“基础解析”“增强解析”还是“增强降级可用”。

#### 场景:Marker 开关保存成功
- **当** 管理员保存 Marker 总开关配置且后端返回成功
- **那么** 页面必须展示成功反馈，并在概览区显示当前模式摘要与风险提示

## 移除需求
<!-- 无 -->
