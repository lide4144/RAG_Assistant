## 1. 配置治理基线

- [x] 1.1 盘点当前 `default.yaml`、`llm_runtime_config.json`、`pipeline_runtime_config.json` 与环境变量涉及的字段，并形成字段 owner 初版分类清单
- [x] 1.2 为新增或现有关键字段补充 `static`、`runtime`、`env_only` 的治理归属规则，并在实现入口统一引用
- [x] 1.3 定义运行时字段是否允许环境变量覆盖的明确边界，避免继续出现隐式覆盖逻辑

## 2. 后端配置来源统一

- [x] 2.1 调整 LLM 与 Planner Runtime 配置加载链路，使六个 stage 与顶层规划模型的运行时配置来源与回退语义可被统一解释
- [x] 2.2 调整 Pipeline runtime 配置解析链路，使 Marker tuning 与 Marker LLM 字段来源能够稳定区分 `default`、`runtime` 与 `env`
- [x] 2.3 为关键配置读取路径补充字段来源与回退行为的测试，覆盖缺失配置、损坏配置和环境变量覆盖场景

## 3. 运行态概览与接口语义

- [x] 3.1 扩展运行态概览或相关管理接口，使关键 runtime 字段能够返回一致的来源语义
- [x] 3.2 统一前后端使用的来源标识与文案，避免同一字段在不同接口中出现不同解释
- [x] 3.3 补充接口契约测试，验证来源标识、回退行为和异常时的可观测输出

## 4. 前端设置页边界与体验

- [x] 4.1 调整“模型设置”页信息架构，明确页面管理的是运行时设置而不是全量系统配置，并为 Planner Runtime 提供独立高风险面板
- [x] 4.2 保持设置页后续现代化实现对 `magic-mcp` 的兼容约束，并在页面文案与注解中保持中文风格
- [x] 4.3 校验设置页在存在环境变量覆盖时能够正确展示字段来源，避免误导管理员认为前端保存值已直接生效

## 5. 验证与文档

- [x] 5.1 更新相关运维或开发文档，说明配置 owner、优先级、Planner Runtime 成本责任和前端设置边界
- [x] 5.2 执行变更涉及的后端测试与前端回归验证，确认配置来源语义未破坏现有功能
- [x] 5.3 复核 OpenSpec 产出物与实现范围一致后，再进入 `/opsx:apply` 实施阶段
