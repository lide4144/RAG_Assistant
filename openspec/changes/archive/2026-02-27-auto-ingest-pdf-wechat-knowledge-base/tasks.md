## 1. URL + PDF 统一入库

- [x] 1.1 扩展 ingest 输入协议，支持 PDF 目录与 URL 列表的统一解析与批处理编排
- [x] 1.2 实现 URL 抓取与正文抽取管线（首版覆盖公众号常见场景）并输出结构化失败原因
- [x] 1.3 统一文档映射产物字段（含 `source_type`、`source_uri`）并保持与既有 `paper_id/title/path` 兼容

## 2. 论文级摘要产物

- [x] 2.1 新增 `paper_summary` 数据模型与落盘逻辑（摘要、要点、关键词、来源、版本）
- [x] 2.2 增加 `chunk_snapshot_hash`（或等价字段）并实现摘要过期检测/重建触发
- [x] 2.3 提供 `paper_summary` 检索读取接口，供检索链路进行候选文档召回

## 3. 双层检索与语义匹配

- [x] 3.1 在检索链路接入“summary 粗召回 -> chunk 精检索/重排”默认流程
- [x] 3.2 引入轻量 embedding 相似度替代纯 `_topic_match_score` 主判定，并实现策略档位阈值配置（strict/balanced/explore）
- [x] 3.3 实现摘要层不可用回退路径（自动切换到原有 chunk 全库检索）
- [x] 3.4 扩展 QA 输出观测字段（摘要召回、候选规模、语义相似度分数、策略档位、回退标识）

## 4. 证据输出与降级策略

- [x] 4.1 保持 `answer_citations` 仅引用 chunk 证据，禁止 summary 直接进入最终引用
- [x] 4.2 将证据不足降级改为语义判定驱动，输出结构化 `answer/refuse/clarify` 与 `reason/missing_aspects`
- [x] 4.3 增加引用映射不完整时的低置信提示，不因单点映射缺口直接硬拒答

## 5. Session State 现代化

- [x] 5.1 将会话主存储迁移到 Redis（含 TTL、并发隔离、会话键约定）
- [x] 5.2 实现分层记忆组装（recent turns + summary memory + semantic recall memory）
- [x] 5.3 引入显式对话状态机字段（如 `normal/need_clarify/waiting_followup`）并替代分散 if-else 状态判断
- [x] 5.4 提供旧会话兼容读取与灰度切换开关，支持安全回滚

## 6. 查询重写与语义路由

- [x] 6.1 接入小模型独立问题重写（Standalone Query Rewrite）并保留失败回退路径
- [x] 6.2 保证改写结果实体保真（模型名/数据集/时间约束）并增加改写质量观测字段
- [x] 6.3 将控制意图升级为语义路由 + 结构化输出（`intent + params`）
- [x] 6.4 实现语义路由失败回退与可观测字段（路由来源、置信度、回退标识）

## 7. UI 与用户体验收口

- [x] 7.1 调整导入反馈文案：明确成功/失败数量、失败原因与下一步建议
- [x] 7.2 在用户视图展示论文级摘要作为导航信息，同时维持引用可交互定位到 chunk 证据
- [x] 7.3 将高级调试信息继续下沉到高级模式，避免干扰默认用户流程

## 8. 测试、验收与发布门槛

- [x] 8.1 新增 URL 导入回归测试（成功、失败、正文为空、访问受限）
- [x] 8.2 新增双层检索与语义匹配回归测试（中英混杂、同义表达、回退路径）
- [x] 8.3 新增 Redis 并发会话与状态恢复测试（清空隔离、挂起恢复、跨会话不污染）
- [x] 8.4 新增重写质量与语义路由准确率评测，并定义告警阈值
- [x] 8.5 新增引用合规测试，确保 `answer_citations` 始终可追溯至 chunk 证据
- [x] 8.6 产出发布检查清单与回滚演练记录（可一键切回旧路径）
