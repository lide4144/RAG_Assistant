## 新增需求
<!-- 无 -->

## 修改需求

### 需求:会话状态脱水存储
系统必须将会话状态主存储从本地文件升级为 Redis（或等价并发安全存储），并支持 TTL 过期与并发写入一致性；禁止继续依赖单机 JSON 文件作为生产主存储。

#### 场景:并发写入不互相覆盖
- **当** 同一时段存在多个会话并发写入
- **那么** 系统必须保证会话隔离与写入一致性，不得出现跨会话覆盖

### 需求:滑动窗口历史供重写
系统必须将上下文组装升级为分层记忆：`recent turns + summary memory + semantic recall memory`，禁止仅依赖固定轮次滑窗作为唯一上下文来源。

#### 场景:长对话跨轮召回历史设定
- **当** 用户在早期轮次提及关键设定并在后续轮次引用
- **那么** 系统必须能通过分层记忆恢复该设定并用于当前轮推理

### 需求:会话清空与隔离
系统必须支持会话显式重置，并在 Redis 与记忆层同步清除（或逻辑隔离）该会话上下文，防止历史污染新会话。

#### 场景:清空后不污染新检索
- **当** 用户执行“新对话/清空上下文”后立即提问无关问题
- **那么** 系统必须按空上下文处理且不复用旧会话记忆

### 需求:系统必须维护主题级澄清计数状态
系统必须在显式状态机中维护澄清状态（例如 `normal/need_clarify/waiting_followup`）与主题级计数，避免通过分散 if-else 隐式维护状态。

#### 场景:澄清状态可恢复
- **当** 上一轮进入 `waiting_followup` 且用户补充信息
- **那么** 系统必须从挂起状态恢复并继续执行后续链路

## 移除需求
<!-- 无 -->
