## 新增需求
<!-- 无 -->

## 修改需求

### 需求:Query Rewriting 基础产出
系统必须支持独立问题重写（Standalone Query Rewrite）：将用户最新问题改写为不依赖历史指代、可直接检索的完整问题；禁止仅通过历史实体字符串拼接构造检索语句。

#### 场景:指代问题改写为独立问题
- **当** 用户输入“它的参数量是多少？”且历史主题为 BERT
- **那么** 系统必须将查询改写为语义完整的独立问题（例如“BERT 模型的参数量是多少”）

### 需求:术语保留策略
系统在独立问题重写中必须保留关键实体与约束（模型名、任务名、时间/数据集限定），不得因改写丢失核心检索锚点。

#### 场景:关键实体在改写后保真
- **当** 原问题包含明确模型或数据集实体
- **那么** 改写结果必须保留该实体并用于后续检索

### 需求:可选 LLM 改写开关
系统必须将独立问题重写配置化，支持开关与失败回退路径；当重写调用失败时，系统必须回退到最小可用查询并记录失败原因。

#### 场景:重写失败回退
- **当** 重写模型调用超时或返回非法输出
- **那么** 系统必须回退到原问题或安全重写结果并保持流程可用

## 移除需求

### 需求:基于历史实体的机械拼接主路径
**Reason**: 机械拼接会破坏自然语义，在密集检索中召回效果不稳定。
**Migration**: 使用小模型独立问题改写替代主路径，并保留拼接策略仅作为紧急回退。
