## 1. 后端模型探测接口

- [x] 1.1 在 Python FastAPI 服务中新增 `POST /api/admin/detect-models` 请求模型与参数校验
- [x] 1.2 使用 `httpx` 实现 `${api_base}/models` 调用，注入 Bearer 鉴权与统一超时配置
- [x] 1.3 实现探测响应标准化（提取并去重模型 `id`）与上游异常格式校验
- [x] 1.4 实现错误映射（参数错误、`AUTH_FAILED`、`UPSTREAM_TIMEOUT`、网络错误、上游非法响应）
- [x] 1.5 为管理接口配置 CORS 允许来源与预检支持，确保浏览器可调用

## 2. 配置持久化与 Router 接入

- [x] 2.1 设计并实现 LLM 管理配置持久化结构（`api_base`、`api_key`、`model`）及读写函数
- [x] 2.2 新增保存配置接口（管理端）并对返回内容做 `api_key` 脱敏
- [x] 2.3 在 LiteLLM 路由配置加载流程接入“动态配置优先，静态配置回退”策略
- [x] 2.4 为持久化配置读取失败场景增加告警与自动回退逻辑

## 3. 前端设置面板与交互

- [x] 3.1 在前端新增 LLM 设置面板 UI（API Base、API Key、测试连接按钮）
- [x] 3.2 接入“测试连接并获取模型”调用并处理加载态、成功态、失败态
- [x] 3.3 在探测成功后渲染模型 Select 下拉并维护用户选择状态
- [x] 3.4 新增保存按钮并提交最终配置到后端保存接口，展示保存结果反馈
- [x] 3.5 根据错误码实现可理解错误提示（如 `AUTH_FAILED`、`UPSTREAM_TIMEOUT`）

## 4. 测试与验收

- [x] 4.1 为 `/api/admin/detect-models` 编写单元/集成测试（成功、401/403、超时、网络错误、非法响应）
- [x] 4.2 为配置持久化与 Router 回退逻辑编写测试（有效配置、损坏配置、缺失配置）
- [x] 4.3 为前端设置面板编写交互测试（探测成功后选模型、保存成功/失败提示）
- [x] 4.4 补充运维文档：动态配置生效路径、回滚步骤与安全注意事项
