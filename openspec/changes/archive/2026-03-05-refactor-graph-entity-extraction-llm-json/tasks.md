## 1. 模型与配置准备

- [x] 1.1 在 `app/graph_build.py`（或配套模块）定义 Pydantic 实体模型，包含 `entity_name` 与 `entity_type` 字段及结果容器
- [x] 1.2 增加 LLM 调用所需配置项（`base_url`、`api_key`、`model`、`timeout`、`max_concurrency`）并接入现有配置加载链路

## 2. 结构化 LLM 抽取实现

- [x] 2.1 实现单 chunk 异步抽取函数，调用 OpenAI 兼容接口并强制 JSON 输出
- [x] 2.2 在单 chunk 抽取中加入 JSON 解析与 Pydantic 校验，返回标准化实体结果或失败标记
- [x] 2.3 移除缺失 `entities` 分支中的正则/规则抽取路径，切换为 LLM 结构化抽取路径

## 3. 并发与容错控制

- [x] 3.1 实现批量抽取协程，使用 `asyncio.Semaphore` + `asyncio.gather` 控制并发并聚合结果
- [x] 3.2 为 429、超时、响应格式错误等异常增加重试/降级处理，确保单 chunk 失败不阻断全局构图
- [x] 3.3 增加关键可观测日志/指标（调用次数、失败类型、降级次数、耗时）以支持上线验证

## 4. 验证与回归

- [x] 4.1 补充或更新针对实体抽取与共现建边输入契约的测试（含成功与异常场景）
- [x] 4.2 在样本数据上执行图构建回归，验证实体抽取成功率、构图稳定性和耗时在可接受范围
