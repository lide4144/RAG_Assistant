## 新增需求
<!-- 如果有新增，请在此处添加完整的需求描述和场景。 -->

## 修改需求

### 需求:系统必须支持保存最终配置
系统必须在模型设置页使用与聊天页和全局壳层一致的部署友好管理接口寻址策略。未显式配置 `NEXT_PUBLIC_KERNEL_BASE_URL` 时，模型设置页的配置读取、保存、模型探测与运行态概览请求必须优先走当前站点下的受控相对 `/api/admin/*` 路径，禁止默认请求 `127.0.0.1`、`0.0.0.0` 或浏览器本机回环地址。

#### 场景:远程部署下打开模型设置页
- **当** 用户通过远程域名或 HTTPS 代理访问“模型设置”页面且未显式配置 `NEXT_PUBLIC_KERNEL_BASE_URL`
- **那么** 页面必须仍能通过当前站点下的受控 `/api/admin/*` 路径读取和保存配置，而不是向浏览器本机回环地址发起请求

#### 场景:远程部署下探测模型
- **当** 用户在模型设置页点击“测试连接”或触发模型探测
- **那么** 系统必须通过部署友好的管理接口地址访问 `/api/admin/detect-models`，且不得因为默认回环地址导致浏览器 CORS 或 loopback address space 拒绝

## 移除需求
<!-- 如果有移除，请在此处添加 -->
