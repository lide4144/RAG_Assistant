## 1. 论文存储访问层

- [x] 1.1 实现论文主记录的查询接口（按 `paper_id`、按来源指纹、按状态筛选）
- [x] 1.2 实现论文主记录的写入接口（创建、更新元数据、更新阶段状态）
- [x] 1.3 实现产物归属记录的读写接口（chunk、summary、structure、索引状态）

## 2. 导入流程重构

- [x] 2.1 修改去重逻辑，使其查询 SQLite 而非 `papers.json`
- [x] 2.2 修改导入成功后的主记录写入，优先写入 SQLite
- [x] 2.3 修改各阶段状态更新（parse、clean、index、graph_build）回写数据库
- [x] 2.4 实现从数据库生成 `papers.json` 兼容导出文件的逻辑
- [x] 2.5 验证导入流程在失败时能正确标记论文状态为 `failed`

## 3. 目录查询重构

- [x] 3.1 修改论文列表查询，从 SQLite 分页读取并支持状态筛选
- [x] 3.2 修改论文详情查询，从数据库读取元数据与生命周期状态
- [x] 3.3 修改专题归属查询，从数据库读取 topic 关联
- [x] 3.4 保留文件产物读取作为降级路径，但标记为 deprecated

## 4. 任务状态聚合重构

- [x] 4.1 修改任务完成后的状态回写，更新单篇论文生命周期状态
- [x] 4.2 修改批任务视图，使其能够关联并展示涉及的论文列表及状态
- [x] 4.3 实现按 `paper_id` 查询论文失败原因与重试状态的接口

## 5. 删除与重建流程

- [x] 5.1 实现删除操作的数据库状态编排（标记 `deleted`、清理产物归属）
- [x] 5.2 实现重建操作的请求处理（标记 `rebuild_pending`）
- [x] 5.3 实现重建任务调度，能够按论文优先级与状态执行重建

## 6. 验证与回滚

- [x] 6.1 为论文存储访问层编写单元测试
- [x] 6.2 验证导入→目录→查询核心链路的稳定性
- [x] 6.3 验证删除、重建等维护操作的正确性
- [x] 6.4 测试文件产物降级路径的可用性（模拟数据库故障场景）
- [x] 6.5 编写迁移与回滚说明文档
