## 修改需求

### 需求:关键词扩展策略
系统必须支持基于同义词/近义词/领域词典的关键词扩展，并将扩展结果写入 `keywords/entities`。扩展实现必须支持中文术语与英文复合术语场景，避免扩展在中文问题中失效。

#### 场景:中文同义词扩展命中
- **当** 问题文本包含中文术语且该术语存在于改写词典（例如“引用”）
- **那么** 系统必须在 `keywords/entities` 中输出对应扩展词（如 `reference`、`citation`）

#### 场景:复合术语不拆分
- **当** 问题包含复合术语（如 `GUESS-18`、`PLA/BPST`、`Top-1`）
- **那么** 系统必须在关键词抽取阶段保留该术语整体，不得拆成无语义碎片

### 需求:问句转检索句策略
系统必须将冗余问句转换为检索导向表达，保留核心语义并删除无效礼貌/冗余成分。系统必须在关键词预算有限时优先保留有效术语，禁止由无意义停用词主导关键词集合。

#### 场景:关键词预算抗噪
- **当** 问句包含大量停用词或礼貌词
- **那么** 系统必须过滤这些噪声词，并优先保留可检索术语进入关键词集合

## 新增需求

### 需求:扩展命中可追踪
系统必须仅在实际新增扩展词时记录 `keyword_expansion` 命中，禁止在未新增扩展词时错误标记该策略命中。

#### 场景:扩展命中与实际新增一致
- **当** 扩展流程未产生任何新增关键词
- **那么** `strategy_hits` 中必须不包含 `keyword_expansion`
