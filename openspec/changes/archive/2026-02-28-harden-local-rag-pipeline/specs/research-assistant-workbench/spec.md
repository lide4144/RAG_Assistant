## 新增需求

### 需求:长耗时任务必须提供可观察进度
系统必须在导入、解析、索引等长耗时本地任务中提供实时进度反馈，禁止仅显示无限期等待状态。

#### 场景:导入流程显示阶段进度
- **当** 用户发起论文导入并进入多阶段处理
- **那么** 界面必须持续显示阶段进度与当前处理状态

### 需求:推理失败必须可恢复且不破坏会话
系统必须捕获推理与外部模型调用异常并以用户可理解方式提示，禁止因单次失败导致界面崩溃或会话重置。

#### 场景:网络波动导致模型调用超时
- **当** 本地应用调用外部模型发生超时或断连
- **那么** 界面必须显示可恢复错误提示并保留当前历史对话内容

### 需求:UI 会话状态必须控制内存体积
系统必须将 `session_state` 限制为渲染必需轻量字段，重型 trace 与检索中间结果必须落盘并按需懒加载。

#### 场景:多轮对话后的状态体积受控
- **当** 用户连续进行多轮问答
- **那么** 系统内存中的会话状态必须不随历史 trace 线性膨胀且可按需查看磁盘明细

## 修改需求
<!-- 无 -->

## 移除需求
<!-- 无 -->
