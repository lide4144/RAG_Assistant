## 1. 原子写入与并发互斥基础设施

- [x] 1.1 在 `app` 中新增通用原子写入工具（同目录临时文件 + 原子替换 + 异常清理）
- [x] 1.2 将 `chunks.jsonl` 与 `papers.json` 的写入路径切换到原子写入工具
- [x] 1.3 为落盘与索引更新入口接入进程级文件锁，并定义锁粒度与超时策略
- [x] 1.4 为并发冲突场景补充用户可理解的错误提示与日志字段

## 2. 大文件序列化与校验稳健性

- [x] 2.1 重构大体量 JSON 输出为流式写入，移除全量 `list + dumps` 内存路径
- [x] 2.2 在 JSONL 写入链路增加逐条序列化容错与失败清理
- [x] 2.3 将 chunk 校验升级为字段存在 + 类型 + 语义约束校验
- [x] 2.4 为校验器实现 `max_errors` fail-fast 机制并输出截断摘要
- [x] 2.5 评估并接入 `orjson` 可选加速路径，保留标准库回退

## 3. 运行目录与路径健壮性改造

- [x] 3.1 在 QA/导入调用入口引入显式 `run_id` 生成与 `run_dir` 传递
- [x] 3.2 移除 UI 侧基于“最新目录”的结果推断逻辑，改为按显式路径读取产物
- [x] 3.3 将默认 `runs`、`data/processed` 等路径改为基于项目根的绝对锚定
- [x] 3.4 在非项目根目录启动场景验证路径解析一致性

## 4. UI 体验与内存控制

- [x] 4.1 将问答输出改为 UI 侧增量渲染，确保流式回答可实时显示
- [x] 4.2 为导入/解析/索引流程增加阶段性进度回调并在界面展示
- [x] 4.3 统一捕获模型调用超时/断连异常，使用可恢复错误提示替代页面崩溃
- [x] 4.4 瘦身 `st.session_state`，仅保留渲染必要字段并将 trace 明细下沉磁盘
- [x] 4.5 实现 trace 详情按需懒加载入口，避免历史会话导致内存线性膨胀

## 5. 测试与验收

- [x] 5.1 新增原子写入中断测试，验证失败后无半成品污染且旧版本可用
- [x] 5.2 新增并发导入/索引测试，验证文件锁互斥与冲突提示行为
- [x] 5.3 新增类型校验与 fail-fast 测试，覆盖错误阈值触发后的提前终止
- [x] 5.4 新增网络波动/模型超时恢复测试，验证错误可恢复且历史会话不断档
- [x] 5.5 新增 UI 流式输出与进度反馈测试，验证长耗时任务可观察性
- [x] 5.6 新增非根目录启动测试，验证路径锚定与默认资源定位稳定
