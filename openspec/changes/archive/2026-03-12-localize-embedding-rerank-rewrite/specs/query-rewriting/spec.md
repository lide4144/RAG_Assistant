## 新增需求
<!-- 无 -->

## 修改需求

### 需求:可选 LLM 改写开关
系统必须将独立问题重写配置化，支持开关与失败回退路径；当重写调用失败时，系统必须回退到最小可用查询并记录失败原因。系统必须支持 rewrite 使用独立模型路由，禁止隐式复用 answer 路由作为唯一来源。

#### 场景:rewrite 独立路由生效
- **当** rewrite 位点配置了独立 provider/api_base/model
- **那么** 系统必须按 rewrite 独立路由执行改写并在诊断信息中反映该位点

#### 场景:rewrite 本地模型不可用时回退
- **当** rewrite 本地模型调用失败、超时或认证失败
- **那么** 系统必须按既有回退策略返回可用改写结果并记录失败原因

## 移除需求
<!-- 无 -->
