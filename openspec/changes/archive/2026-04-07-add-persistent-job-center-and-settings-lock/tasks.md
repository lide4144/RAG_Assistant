## 1. 数据层与统一任务模型

- [x] 1.1 为 `job`、`job_event` 与配置冻结快照/版本新增持久化结构，并提供最小读写封装
- [x] 1.2 将现有图构建与论文导入任务映射到统一任务字段，补齐 `jobId/taskKind/state/updatedAt` 等基础契约
- [x] 1.3 为统一任务模型补充单元测试，覆盖状态迁移、事件顺序与恢复查询基础行为

## 2. Kernel/API 任务化与配置冻结

- [x] 2.1 为聊天问答与规划问答新增后台任务创建入口，返回稳定 `jobId` 并记录 `sessionId/traceId/runId`
- [x] 2.2 为统一任务新增查询、事件回放与取消接口，支持按 `jobId` 和事件游标恢复
- [x] 2.3 在聊天/规划任务启动时冻结有效配置，并将配置版本或配置快照绑定到任务记录
- [x] 2.4 为模型设置保存接口增加活动任务占用检查，并返回 `SETTINGS_LOCKED_BY_ACTIVE_JOB` 结构化错误
- [x] 2.5 为 Kernel 任务接口补充契约测试，覆盖聊天任务恢复、配置冻结与设置锁拒绝场景

## 3. Gateway 实时分发与恢复桥接

- [x] 3.1 将聊天与规划流式事件桥接到统一 `job_event` 语义，同时保持现有前端可消费事件格式兼容
- [x] 3.2 支持按 `jobId` 重连补拉缺失事件，并在恢复后继续转发实时事件
- [x] 3.3 为 Gateway 增加集成测试，覆盖首次订阅、断连恢复与事件顺序一致性

## 4. 前端任务中心与聊天恢复

- [x] 4.1 在 `AppShell` 或等价全局层引入 `TaskCenterProvider`，维护活动任务、最近事件游标与恢复逻辑
- [x] 4.2 将 `ChatShell` 改为发起 `chat_answer/planner_chat` 后台任务，并基于 `jobId` 恢复回答增量、来源与调试信息
- [x] 4.3 将 `PipelineShell` 接入统一任务中心，复用同一活动任务摘要与恢复逻辑
- [x] 4.4 为前端任务中心补充页面切换、刷新恢复与同一消息连续追加的端到端测试

## 5. 设置锁定与运行态交互

- [x] 5.1 让 `SettingsShell` 读取活动任务锁状态，在存在活动任务时进入只读模式并展示占用任务摘要
- [x] 5.2 禁用设置页中所有会改变运行时配置的输入、检测与保存操作，并正确展示后端锁错误
- [x] 5.3 为设置页补充测试，覆盖活动任务锁定、保存被拒绝与任务结束后恢复可编辑状态

## 6. 可观测性与收尾验证

- [x] 6.1 将 `jobId` 与现有 `traceId/runId`、模型调试记录对齐，确保恢复后仍可查看同一任务的调试上下文
- [x] 6.2 增加任务与事件清理策略或运行说明，避免长期累积的事件日志无限增长
- [x] 6.3 运行后端、Gateway、前端的最小回归测试集合，确认聊天恢复、设置锁与现有任务面板不回退
